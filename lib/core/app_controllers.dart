import 'package:flutter/material.dart';

class AppControllers {
  static final PageController homeController = PageController(initialPage: 0);
  static final PageController profileController =
      PageController(initialPage: 0);
}
